/// Default settings for the package
class PackageDefaultSettings {
  /// Default output directory for generated files
  static const String outputDirectory = 'lib/l10n';

  /// Default filename prepend text
  static const String filenamePrepend = '';

  /// Default Excel sheet name for translations
  static const String defaultSheetName = 'Translations';
}
